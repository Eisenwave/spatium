package net.grian.spatium.util;

@SuppressWarnings("unused")
public final class Bytes {
    
    public final static int
    OBJECT_BYTES = 8,
    REFERENCE_BYTES = 4,
    LONG_BYTES = Long.BYTES,
    INT_BYTES = Integer.BYTES,
    SHORT_BYTES = Short.BYTES,
    CHAR_BYTES = Character.BYTES,
    BYTE_BYTES = Byte.BYTES,
    BOOLEAN_BYTES = BYTE_BYTES,
    DOUBLE_BYTES = Double.BYTES,
    FLOAT_BYTES = Double.BYTES;
    
}
